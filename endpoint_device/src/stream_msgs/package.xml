<?xml version="1.0"?>
<package format="2">
  <name>stream_msgs</name>
  <version>1.0.0</version>
  <description>
    Shared messages for the endpoint-to-edge H.264 video stream:
    per-frame metadata, adaptive stream control and motion-gate stats.
    NOTE: this package is duplicated in endpoint_device/src and
    edge_device/src. Both copies MUST stay identical so message
    MD5 sums match across devices.
  </description>

  <maintainer email="yukki@todo.todo">yukki</maintainer>
  <license>MIT</license>

  <buildtool_depend>catkin</buildtool_depend>
  <build_depend>message_generation</build_depend>
  <build_depend>std_msgs</build_depend>
  <exec_depend>message_runtime</exec_depend>
  <exec_depend>std_msgs</exec_depend>

  <export />
</package>
